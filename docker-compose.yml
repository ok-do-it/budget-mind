services:
  postgres:
    image: postgres:16
    container_name: budget-mind-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: budget_mind
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
